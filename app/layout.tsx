import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

export const metadata: Metadata = {
  title: "Dr. Serena Blake, PsyD | Clinical Psychologist in Los Angeles, CA",
  description:
    "Dr. Serena Blake is a licensed clinical psychologist (PsyD) in Los Angeles, CA, offering in-person and virtual therapy for anxiety, relationships, and trauma recovery. 8 years of experience, 500+ sessions. Schedule your session today!",
};

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased overflow-x-hidden`}
      >
        {children}
      </body>
    </html>
  );
}
